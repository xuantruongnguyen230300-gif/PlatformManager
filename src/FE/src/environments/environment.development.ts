export const environment = {
  production: false,
  // Khớp server local backend-expert đang chạy (xem doc/contracts/*.md) —
  // đổi lại nếu backend đổi port/scheme khi lên môi trường khác.
  apiBaseUrl: 'http://localhost:5027/api'
};
