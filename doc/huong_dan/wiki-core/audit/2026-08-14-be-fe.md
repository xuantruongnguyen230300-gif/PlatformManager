# Core Compliance Report — 2026-08-14 — phạm vi: cả BE và FE

> Chuẩn đối chiếu: `doc/huong_dan/wiki-core/be/01..10-*.md` (BE) và
> `src/FE/.claude/docs/*.md` (FE — chuẩn **tạm thời**, vì
> `wiki-core/fe/README.md` còn là stub, xem § Giới hạn phạm vi).
> Root đã resolve: `{BE_ROOT}` = `src/BE/` (marker: `PlatformManager.slnx` —
> **không phải** `.sln`, xem § Ghi chú resolve root), `{FE_ROOT}` = `src/FE/`
> (marker `angular.json` ✅).
> Reviewer chỉ đọc code, không sửa bất kỳ file nào trong `src/BE`/`src/FE`.

## Kết luận: PARTIAL

Phần core **đã có nền tốt và nhất quán ở tầng vỏ** (envelope response,
exception middleware, soft-delete global filter, ranh giới DTO/model FE,
Angular 20 convention) — nhưng **lệch khỏi chính bộ quy ước thực thi của
mình ở tầng sâu** (không có `Result<T>`, không có factory method/private
setter, không có tầng Domain/Application/CQRS như `src/BE/.claude/rules/*`
mô tả), và **không có bất kỳ test nào** ở cả 2 phía để giữ các quy tắc đó
khỏi trôi tiếp.

Không đánh BLOCKED vì: các lệch pha đều nằm trong vùng "demo scale, sửa
được tăng dần", không có lỗ hổng bảo mật đang khai thác được ở môi trường
local. Nhưng có **2 mục bắt buộc phải hỏi người dùng trước khi bất kỳ agent
nào sửa** (§ Cần người dùng quyết định) — không agent nào được tự chọn phe.

Số finding: **13** (MISSING 6 / PARTIAL 7) + 2 mục chờ user quyết định + 6
mục "chưa tới ngưỡng — không tính là finding".

---

## 🛑 Cần người dùng quyết định (KHÔNG agent nào tự quyết)

### Q1 — Quy ước thực thi BE mâu thuẫn trực tiếp với code hiện tại: Clean Architecture + CQRS hay không?

Đây không phải finding của riêng agent nào — đây là mâu thuẫn giữa 2 nguồn
sự thật, và **không rõ bên nào đúng**:

| Nguồn | Nói gì |
| --- | --- |
| `src/BE/CLAUDE.md:16-17` | "Clean Architecture: Domain / Application / Infrastructure / Api" + "CQRS-lite qua MediatR: mỗi use case = 1 Command/Query + 1 Handler" |
| `src/BE/.claude/rules/architecture.md:5-13` | Layout 4 project + `PlatformManager.sln` |
| `src/BE/.claude/rules/cqrs-handler.md:1-117` | Toàn bộ file mô tả MediatR + `Result<T>` |
| `.claude/agents/backend-expert.md:18-19, 66, 96` | Cùng nội dung trên |
| **Code thật** | 1 project duy nhất `PlatformManager.Api` (`PlatformManager.slnx` chỉ liệt kê 1 csproj); không có MediatR trong `PlatformManager.Api.csproj:9-18`; controller inject thẳng `AppDbContext` (`Controllers/CriteriaController.cs:13`); logic nghiệp vụ nằm ở `Services/*.cs` |
| **Bằng chứng "cố ý"** duy nhất | 1 câu comment trong code: `Entities/BaseEntity.cs:7` — "demo đơn giản, không dùng factory method/CQRS" |

Đã tìm khắp `doc/`, `.claude/`, `src/BE/.claude/` — **không có ADR, không có
dòng nào trong tài liệu ghi nhận quyết định bỏ Clean Architecture/CQRS**.
Nghĩa là không thể kết luận đây là "đơn giản hoá đã được thống nhất"
(không tính là finding) hay "thiếu sót thật" (là finding). Toàn bộ các
finding BE-1, BE-2, BE-5 bên dưới phụ thuộc vào câu trả lời này.

**Hỏi người dùng chọn 1:**
- (a) Code đúng → cần **sửa lại tài liệu**: `src/BE/CLAUDE.md`,
  `src/BE/.claude/rules/architecture.md`, `.claude/rules/cqrs-handler.md`,
  `.claude/agents/backend-expert.md` để mô tả đúng kiến trúc "1 project,
  service-based" + ghi 1 ADR giải thích lý do.
- (b) Tài liệu đúng → cần **tái cấu trúc lớn** (tách 4 project, chuyển
  Services → MediatR handler, thay `ApiException` → `Result<T>`). Đây là
  thay đổi nhiều file, nhiều tầng — reviewer **không** đề xuất
  `backend-expert` làm ngay mà không có quyết định của user.
- (c) Giữ nguyên code, giữ nguyên tài liệu như "đích đến tương lai", nhưng
  ghi rõ trong `src/BE/CLAUDE.md` rằng bản demo hiện tại **cố ý** chưa theo,
  kèm điều kiện khi nào chuyển.

### Q2 — Auth: wiki nói "đã bỏ qua", quy ước BE nói "đã CHỐT ASP.NET Core Identity", code là bảng lookup trơ

| Nguồn | Nói gì |
| --- | --- |
| `wiki-core/be/01-core-components.md:32` | "Chưa có #7 (đã bỏ qua theo yêu cầu demo)" |
| `src/BE/CLAUDE.md:20-22` | "**ASP.NET Core Identity** cho auth (**đã chốt**). Entity `AppUser` (`IdentityUser<Guid>`)" |
| `src/BE/.claude/rules/api-controller.md:113-124` | "**Đã CHỐT: ASP.NET Core Identity**", `DbContext` kế thừa `IdentityDbContext<...>` |
| **Code thật** | `Entities/AppUser.cs:3-14` — "**KHÔNG phải ASP.NET Core Identity** (không password/login field nào)", chỉ có `Id` + `FullName`; `Data/AppDbContext.cs:6` kế thừa `DbContext` thuần; `PlatformManager.Api.csproj` không có package Identity nào |

Thêm 1 chi tiết dễ gây hiểu nhầm: `Program.cs:57` gọi
`app.UseAuthorization()` trong khi **không có `AddAuthentication()`/scheme
nào** — hiện tại là no-op, nhưng đọc lướt `Program.cs` sẽ tưởng API đã được
bảo vệ. Mọi endpoint (kể cả `POST /api/import/csv` nhận file 20MB —
`Controllers/ImportController.cs:12`) đang mở hoàn toàn.

**Hỏi người dùng:** demo có tiếp tục không-auth không? Nếu có → nên sửa
`src/BE/CLAUDE.md` + `api-controller.md` bỏ chữ "đã CHỐT" (hoặc đổi thành
"đã chốt cho giai đoạn sau"), và xoá `app.UseAuthorization()` để không tạo
ảo giác an toàn. Nếu không → đây là việc lớn của `backend-expert`, cần lên
kế hoạch riêng.

---

## Findings — BE

### BE-1 — `be/01-core-components.md` #3 (Factory method + private setter) — "Bắt buộc, ngày đầu"
- **Mức: MISSING**
- **Bằng chứng:**
  - `src/BE/PlatformManager.Api/Entities/BaseEntity.cs:11-14` — cả 4 property
    là `{ get; set; }` **public**, trong khi chính XML doc ngay phía trên
    (`BaseEntity.cs:6-7`) tự khẳng định "Setter là `protected`" → tài liệu
    trong code mô tả sai chính code đó.
  - `Entities/Criteria.cs:10-16`, `Entities/CriteriaAssessment.cs:19-30` —
    toàn bộ public setter, không có `static Create(...)` nào; grep toàn
    `Entities/` không có factory method.
  - Hệ quả thật (không phải lý thuyết): invariant bị áp rải rác thay vì tập
    trung — `Math.Clamp(..., 0, 100)` cho `ProgressPercent` bị lặp ở
    `Controllers/AssessmentsController.cs:45` **và**
    `Services/AssessmentUpsertService.cs:86`; validate `MaxScore > 0` nằm ở
    `Controllers/CriteriaController.cs:120`, không nằm trong entity.
  - `src/BE/.claude/rules/entity-domain.md:16, 23-58` yêu cầu ngược lại.
- **Agent chịu trách nhiệm:** `backend-expert`
- **Gợi ý sửa:** đổi `BaseEntity` sang `{ get; protected set; }`; thêm
  `Criteria.Create(code, name, groupId, maxScore)` +
  `Criteria.Update(...)`, `CriteriaAssessment.Create(...)` +
  `SetProgress(decimal)` (gom `Math.Clamp` vào đúng 1 chỗ); đổi 2 chỗ khởi
  tạo bằng object-initializer (`CriteriaController.cs:50-58`,
  `AssessmentUpsertService.cs:52-57`) sang gọi factory. Việc này **không**
  phụ thuộc Q1 — làm được kể cả khi giữ kiến trúc 1 project.

### BE-2 — `be/01-core-components.md` #5 (Error-as-value `Result<T>`) — "Bắt buộc, ngày đầu"
- **Mức: PARTIAL** (nửa sau của #5 — exception middleware — đã PASS)
- **Bằng chứng:** grep `Result<` trong toàn bộ `PlatformManager.Api` (trừ
  `PagedResult`) → **0 kết quả**. Lỗi nghiệp vụ mong đợi đang được `throw`:
  `Controllers/CriteriaController.cs:69` (NotFound), `:91`, `:117-130`
  (BadRequest/Conflict), `Controllers/AssessmentsController.cs:33, 40`,
  `Controllers/ImportController.cs:16`. `Common/ApiResponse.cs:35-43` định
  nghĩa `ApiException` làm cơ chế chính thức cho lỗi mong đợi — đúng cái
  wiki #5 gọi là "exception-driven control flow".
  `src/BE/.claude/rules/cqrs-handler.md:93-94` nói rõ: "Lỗi nghiệp vụ **mong
  đợi** → trả `Result<T>` lỗi, **không throw**".
- **Agent chịu trách nhiệm:** `backend-expert`
- **Gợi ý sửa:** phụ thuộc Q1. Nếu (a)/(c): tối thiểu thêm
  `Common/Result.cs` + `ResultErrorType` + extension `ToApiResponse()` như
  `cqrs-handler.md:75-89` mô tả, chuyển `Services/*` trả `Result<T>`, giữ
  `ApiException` **chỉ** cho chỗ thật sự bất ngờ. Nếu user chọn giữ
  `ApiException` → phải sửa `cqrs-handler.md` cho khớp, không để 2 nguồn
  đá nhau.

### BE-3 — `be/04-testing-strategy.md` (toàn file) — "Ngay từ đầu, chi phí thấp lợi ích ngay" (`be/10:14`)
- **Mức: MISSING**
- **Bằng chứng:** `find src/BE -iname "*test*"` → **0 file**;
  `PlatformManager.slnx` chỉ có đúng 1 project (`PlatformManager.Api`).
  Không có ArchTest, unit test, integration test nào.
- **Vì sao đây là finding chứ không phải "chưa tới ngưỡng":** wiki
  `be/04:7` nêu chính xác nỗi đau đang xảy ra ở repo này — "tài liệu chỉ có
  tác dụng lúc đọc, ArchTest có tác dụng mãi mãi". Finding BE-1 (public
  setter) là ví dụ sống: 1 ArchTest 5 dòng "không entity nghiệp vụ nào có
  public setter" đã chặn được ngay từ commit đầu.
- **Agent chịu trách nhiệm:** `backend-expert`
- **Gợi ý sửa:** thêm `PlatformManager.Tests` (xUnit) vào `.slnx`, bắt đầu
  bằng đúng 3 test: (1) ArchTest "mọi class kế thừa `BaseEntity` không có
  public setter", (2) unit test `AssessmentUpsertService` cho luật
  copy-forward/upsert-trong-ngày (`AssessmentUpsertService.cs:25-93` — logic
  phức tạp nhất BE, hiện 0% test), (3) unit test công thức delta/badge của
  `AggregationService`. **Không** dùng `UseInMemoryDatabase` (`be/04:21`) —
  mock hoặc Testcontainers.PostgreSql.

### BE-4 — `be/08-adr-practice.md` — "Ngay từ đầu, chi phí gần 0" (`be/10:15`)
- **Mức: MISSING**
- **Bằng chứng:** không có thư mục/file ADR nào trong `doc/`
  (`doc/` chỉ có `Design/`, `ERD/`, `Prototype/`, `contracts/`,
  `huong_dan/`). Các quyết định lớn **đã ra** nhưng không được ghi ở dạng
  "bối cảnh → lựa chọn → quyết định → lý do": bỏ Clean Architecture/CQRS
  (chỉ còn 1 comment ở `Entities/BaseEntity.cs:7`), bỏ `AssessmentPeriod`
  (chỉ nhắc ở `Entities/CriteriaAssessment.cs:5-6`), `CreatedAt` vừa là
  audit vừa là field nghiệp vụ (`CriteriaAssessment.cs:8-12`), luật
  hard-delete/soft-delete có điều kiện (`CriteriaController.cs:83-85`).
- **Vì sao đáng sửa ngay:** chính reviewer này đã phải dừng lại và hỏi user
  (Q1) **chỉ vì** thiếu 1 file ADR 5 dòng. Chi phí thiếu ADR đã hiện hữu.
- **Agent chịu trách nhiệm:** `backend-expert` (viết ADR cho quyết định BE);
  quyết định cuối vẫn là của user (xem Q1).
- **Gợi ý sửa:** tạo `doc/adr/0001-kien-truc-1-project-cho-demo.md` (hoặc
  vị trí user chọn), format tối giản 4 dòng như `be/08:5` khuyến nghị; ghi
  ngược lại 4 quyết định đã liệt kê ở trên.

### BE-5 — `be/01-core-components.md` #2 (Generic Repository/UnitOfWork) — "Bắt buộc, ngày đầu"
- **Mức: PARTIAL**
- **Bằng chứng:** không có `IRepository`/`IUnitOfWork` nào; controller inject
  thẳng `AppDbContext` (`Controllers/CriteriaController.cs:13`,
  `Controllers/AssessmentsController.cs:12`) và tự gọi `db.SaveChangesAsync`
  (`CriteriaController.cs:60, 78, 109`).
  Lưu ý công bằng: `DbContext` **chính là** Unit of Work và `DbSet<T>` là
  repository sẵn có của EF Core — nên nỗi đau gốc mà #2 nhắm tới ("viết lại
  CRUD cho mỗi entity") **chưa xảy ra** ở quy mô 4 entity. Cái thật sự lệch
  là quy ước riêng của repo (`.claude/rules/architecture.md:22` cấm chạm EF
  trực tiếp ngoài tầng Infrastructure) — mà tầng đó hiện không tồn tại.
  Ngoài ra `wiki-core/be/01-core-components.md:32` khẳng định PlatformManager
  "đã có #2" — **không khớp** với code (xem § Ghi chú về wiki).
- **Agent chịu trách nhiệm:** `backend-expert` (sau khi user trả lời Q1)
- **Gợi ý sửa:** nếu Q1 → (a)/(c): không cần thêm repository (over-engineering
  ở 4 entity), chỉ cần **ngừng inject `AppDbContext` vào Controller** —
  chuyển 3 thao tác ghi trong `CriteriaController` xuống 1
  `CriteriaService`, để controller mỏng đúng như `api-controller.md:16-17`
  yêu cầu. Nếu (b): làm đúng theo `architecture.md`.

### BE-6 — `be/06-concurrency-control.md` — ngưỡng "entity có khả năng nhiều người cùng sửa"
- **Mức: MISSING**
- **Bằng chứng:** grep `RowVersion|IsConcurrencyToken|DbUpdateConcurrency|xmin`
  → **0 kết quả** trong toàn BE. Đường ghi đè im lặng cụ thể:
  `Services/AssessmentUpsertService.cs:39-45` — nếu đã có record hôm nay thì
  `apply(existing)` + `SaveChanges`, không so sánh phiên bản. Hai người cùng
  mở grid "Danh mục DTI" và sửa cùng 1 ô `ProgressPercent`/`Note` của cùng 1
  chỉ tiêu trong cùng ngày → người lưu sau ghi đè, UI cả 2 đều báo thành
  công. Cùng record còn bị ghi bởi luồng import CSV
  (`Services/CsvImportService.cs:18` dùng chung `AssessmentUpsertService`) →
  import trong lúc có người đang nhập tay cũng ghi đè.
- **Đánh giá ngưỡng (theo `be/06:19`):** `CriteriaAssessment` đúng là "danh
  mục dùng chung, nhiều người cùng sửa" về mặt nghiệp vụ (có cả field
  `OwnerId`/"Phụ trách"). Ngưỡng **đã chạm về nghiệp vụ**; chỉ có thể hoãn
  nếu user xác nhận demo chạy đơn người dùng — nên reviewer đánh MISSING
  chứ không tự hạ xuống "chưa tới ngưỡng".
- **Agent chịu trách nhiệm:** `backend-expert`
- **Gợi ý sửa:** với Postgres dùng `xmin` làm concurrency token (không cần
  đổi schema): `e.Property<uint>("xmin").IsRowVersion().HasColumnName("xmin")`
  trong `AppDbContext.OnModelCreating` cho `CriteriaAssessment`; bắt
  `DbUpdateConcurrencyException` ở `ExceptionMiddleware` → trả 409 +
  `ErrorCode = "ASSESSMENT_CONCURRENT_UPDATE"`; FE hiện thông báo "dữ liệu đã
  bị người khác thay đổi, tải lại trang".

### BE-7 — `be/01-core-components.md` #6 (Envelope cho MỌI endpoint) — lỗ hổng ở nhánh model-binding
- **Mức: PARTIAL** (phần chủ động đã PASS toàn bộ)
- **Bằng chứng:** 12/12 `return` của controller đều bọc `ApiResponse`
  (`CriteriaController.cs:42, 62, 80, 111`; `AssessmentsController.cs:53`;
  `CriteriaGroupsController.cs:21`; `DashboardController.cs:20, 29, 41`;
  `ImportController.cs:20`) — rất tốt, kể cả endpoint list
  (`ApiResponse<PagedResult<T>>`). **Nhưng** `[ApiController]`
  (`CriteriaController.cs:11` v.v.) bật auto-400 trả **ProblemDetails
  RFC7807**, không phải envelope, và `Program.cs:14-18` không hề gọi
  `ConfigureApiBehaviorOptions`/`InvalidModelStateResponseFactory`. Chỉ cần
  client gửi `POST /api/criteria` với `"MaxScore": "abc"` (sai kiểu) là FE
  nhận body `{type, title, status, errors}` — đúng "2 nhánh parse khác nhau"
  mà wiki #6 muốn tránh. Đây cũng là 1 nguyên nhân khiến FE-1 bên dưới càng
  khó phát hiện.
- **Agent chịu trách nhiệm:** `backend-expert`
- **Gợi ý sửa:** trong `Program.cs`, thêm
  `builder.Services.Configure<ApiBehaviorOptions>(o => o.InvalidModelStateResponseFactory = ctx => new BadRequestObjectResult(ApiResponse.Fail("VALIDATION_ERROR", <gộp message từ ModelState>, ctx.HttpContext.TraceIdentifier)));`

### BE-8 — `be/03-metadata-driven-design.md` § "Áp dụng vào PlatformManager", dòng "Ngưỡng badge — Loại (C) — ✅ Nên đưa vào config/`SysConfig`"
- **Mức: PARTIAL** (đây là finding **chung BE + FE**, xem thêm FE-2)
- **Bằng chứng:** ngưỡng badge hard-code ở cả 2 phía, **trùng lặp 8 chỗ**:
  BE `Services/AggregationService.cs:41` (`Epsilon = 0.001m`), `:160`
  (`>= 99.999m`), `:196` (`>= 99.999m`); FE
  `modules/dashboard/components/criteria-table/criteria-table.ts:43-44`,
  `components/kpi-summary/kpi-summary.ts:46-47`,
  `pages/dashboard/dashboard.page.ts:97-100`. Đổi ngưỡng "Hoàn thành" phải
  sửa 2 repo, 6 file — đúng loại nợ mà wiki 03 chỉ mặt.
- **Agent chịu trách nhiệm:** `backend-expert` (nguồn cấu hình + trả xuống
  FE), `frontend-expert` (bỏ hằng số hard-code phía FE)
- **Gợi ý sửa:** BE khai `AssessmentThresholdOptions { DoneAtLeast = 99.999m, DeltaEpsilon = 0.001m }`
  bind từ `appsettings.json` qua `IOptions<T>` (giải quyết luôn BE-9), trả
  kèm trong `GET /api/dashboard/...` hoặc 1 endpoint config nhỏ; FE đọc từ
  đó thay vì viết lại số. Nếu user thấy chưa đáng, tối thiểu gom về **1
  hằng số duy nhất mỗi phía** (`AggregationService` + 1 file constants FE) và
  ghi chú chéo — không để rải 6 chỗ.

### BE-9 — `be/01-core-components.md` #10 (Config/Options abstraction, fail-fast) — "Nên có sớm"
- **Mức: MISSING**
- **Bằng chứng:** grep `IOptions|Configure<` → **0 kết quả**.
  `Program.cs:9-10` đọc `Cors:AllowedOrigins` bằng
  `GetSection(...).Get<string[]>() ?? [...]` — gõ sai key trong appsettings
  sẽ **im lặng rơi về default**, đúng nỗi đau #10 mô tả ("cấu hình sai chỉ
  lộ lúc runtime"). `Program.cs:23` đọc connection string không kiểm tra
  null.
- **Agent chịu trách nhiệm:** `backend-expert`
- **Gợi ý sửa:** gom về `CorsOptions`/`AssessmentThresholdOptions` typed,
  đăng ký bằng `.AddOptions<T>().Bind(...).ValidateDataAnnotations().ValidateOnStart()`
  — 1 dòng `ValidateOnStart()` là toàn bộ giá trị fail-fast của #10. Làm
  chung 1 lượt với BE-8.

### BE-10 — `be/01-core-components.md` #9 (Logging/Audit abstraction) — "Bắt buộc, ngày đầu"
- **Mức: PARTIAL**
- **Bằng chứng:** log kỹ thuật có và đúng chuẩn structured
  (`Common/ExceptionMiddleware.cs:29` — `"Unhandled exception. TraceId={TraceId}"`,
  named placeholder chứ không nối chuỗi ✅; `Data/DbSeeder.cs:29`). Nhưng
  **không có ILogger nào trong 3 service nghiệp vụ**
  (`AggregationService.cs`, `AssessmentUpsertService.cs`,
  `CsvImportService.cs` — grep `ILogger` chỉ ra 3 chỗ, không chỗ nào trong
  `Services/`), và không có audit nghiệp vụ (ai đổi `ProgressPercent` từ
  bao nhiêu sang bao nhiêu). Đặc biệt đáng chú ý ở `CsvImportService` — 1
  lần import ghi đè hàng chục `CriteriaAssessment` mà không để lại dấu vết
  nào ngoài `UpdatedAt`.
- **Ghi chú công bằng:** audit "ai làm" **bị chặn bởi Q2** (chưa có khái
  niệm current user) — không thể trách `backend-expert` phần đó.
- **Agent chịu trách nhiệm:** `backend-expert`
- **Gợi ý sửa:** phần làm được ngay không cần chờ Q2 — thêm `ILogger<T>` vào
  `CsvImportService` (log tổng kết mỗi lần import: tổng dòng/thành
  công/lỗi/số record bị ghi đè) và `AssessmentUpsertService` (log ở nhánh
  ghi đè record đã tồn tại, `:39-45`).

### BE-11 — `be/09-security-beyond-auth.md` § Quản lý secret
- **Mức: PARTIAL** (mức độ thấp, nhưng là thói quen cần chặn sớm)
- **Bằng chứng:** `appsettings.Development.json:9` commit vào git kèm
  password plaintext (`...Username=postgres;Password=postgres`). Đây là
  default local nên rủi ro thực tế thấp, nhưng `be/09:6` nói rõ "không commit
  vào git dạng plaintext". Rủi ro thật hơn: `appsettings.json` **không có**
  `ConnectionStrings` nào → chạy ở môi trường không phải Development sẽ chết
  ở `Program.cs:43` (`MigrateAsync`) với lỗi khó đọc.
- **Agent chịu trách nhiệm:** `backend-expert`
- **Gợi ý sửa:** chuyển connection string dev sang `dotnet user-secrets`
  (hoặc biến môi trường `ConnectionStrings__Default`), giữ
  `appsettings.Development.json` không chứa password; thêm kiểm tra
  fail-fast rõ ràng ở `Program.cs` nếu thiếu connection string (gộp với
  BE-9).

### BE-12 — `be/10-data-retention.md` — "quyết định trước chính sách, thực thi sau"
- **Mức: PARTIAL**
- **Bằng chứng:** soft-delete đã có và hoạt động đúng
  (`AppDbContext.cs:34, 50, 74, 87`), nhưng **chưa có chính sách lưu trữ nào
  được ghi lại**. `CriteriaAssessment` thiết kế theo kiểu tích luỹ 1 bản
  ghi/chỉ tiêu/ngày (`Entities/CriteriaAssessment.cs:8-13`) — 62 chỉ tiêu ×
  365 ngày ≈ 22.6k dòng/năm, tăng tuyến tính vĩnh viễn; `Criteria` bị
  soft-delete thì không bao giờ được dọn (`CriteriaController.cs:98-102`).
- **Agent chịu trách nhiệm:** `backend-expert` (ghi chính sách), user (chốt
  thời hạn lưu)
- **Gợi ý sửa:** đúng như `be/10:8` — **chưa cần code job dọn**, chỉ cần 1
  đoạn trong ADR/`doc/ERD/ERD.md` ghi: giữ `CriteriaAssessment` bao lâu, ai
  được hard-delete `Criteria` đã soft-delete, sau bao lâu.

---

## Findings — FE

> Nhắc lại giới hạn: chuẩn đối chiếu FE là `src/FE/.claude/docs/*.md`
> (Lớp 2/3), **không** phải bộ quy tắc core FE dài hạn — bộ đó chưa tồn tại
> (`wiki-core/fe/README.md` là stub). Các finding dưới đây vì vậy hẹp hơn
> đáng kể so với phần BE, không phản ánh "FE tốt hơn BE".

### FE-1 — `.claude/docs/api-client.md` § "Lỗi HTTP xử lý qua interceptor dùng chung" + envelope contract
- **Mức: PARTIAL** — bug wire boundary thật, đang chạy
- **Bằng chứng:** `core/interceptors/http-error.interceptor.ts:30-32` đọc
  `body.Message` / `body.message`:
  ```ts
  const body = error.error as { Message?: string; message?: string } | null;
  if (body?.Message) return body.Message;
  ```
  Nhưng envelope lỗi của BE **không có field `Message`** — field đúng là
  `ErrorMessage` (`core/http/api-response.model.ts:11`;
  `src/BE/PlatformManager.Api/Common/ApiResponse.cs:12`;
  `doc/contracts/danh-muc-dti.md:27` mẫu response thật). Hệ quả: **mọi
  message nghiệp vụ cụ thể do BE trả về đều bị nuốt** — user thấy
  "Dữ liệu xung đột với bản ghi đã tồn tại." (`:46`) thay vì
  "Mã chỉ tiêu '1.1' đã tồn tại." mà BE đã cất công trả
  (`CriteriaController.cs:130`); tương tự với
  `CRITERIA_ASSESSMENT_READONLY_PERIOD`
  (`AssessmentsController.cs:40-42`) — thông báo hữu ích nhất của tính năng
  bị thay bằng câu chung chung.
  Hệ quả phụ: `core/services/api-response.service.ts:19-28` (nhánh
  `200 OK` + `Success:false`) thực tế **không bao giờ chạy** với BE hiện tại,
  vì BE luôn đổi HTTP status khi lỗi (`ExceptionMiddleware.cs:23, 31`) —
  không phải lỗi, nhưng nên biết để không tưởng đã có 2 lớp bảo vệ.
- **Agent chịu trách nhiệm:** `frontend-expert`
- **Gợi ý sửa:** trong `resolveMessage`, ép kiểu về
  `Partial<ApiResponse<unknown>>` và đọc `ErrorMessage` (giữ fallback theo
  status cho lỗi mạng/ProblemDetails). Bổ sung nhánh đọc `title`/`detail`
  của ProblemDetails cho tới khi BE-7 được sửa. Kèm 1 unit test cho
  `resolveMessage` (giải quyết luôn FE-3).

### FE-2 — `.claude/docs/ui-conventions.md` § Style & Design Tokens
- **Mức: PARTIAL**
- **Bằng chứng:** vi phạm **cả 2 vế** của quy tắc:
  - Hardcode dù token đã có: `--card: #fff` tồn tại (`styles.scss:12`) nhưng
    component vẫn viết `background: #fff` —
    `modules/danh-muc-dti/components/criteria-grid-table/criteria-grid-table.scss:21, 34`;
    `components/editable-cell/editable-cell.scss:47`;
    `shared/components/pagination/pagination.scss:23, 29`;
    `shared/components/sidebar/sidebar.scss:37, 59`;
    `modules/dashboard/components/period-toolbar/period-toolbar.scss:32, 46`.
  - Tự phát minh giá trị mới **không báo cáo** (`ui-conventions.md:50-52`):
    `#f8fafc` (criteria-grid-table.scss:26, 40, 45; report-dialog.scss:4),
    `#cad4e1` (editable-cell.scss:29), `#bfe3d2`/`#e7f7f0`
    (editable-cell.scss:63, 66), `#f3caca`/`#fdecec` (:72, :75),
    `#edf1f6` (group-progress-list.scss:32), `#cbd6e5` (report-dialog.scss:5)
    — 9 màu ngoài hệ token, chưa cái nào được đưa vào `styles.scss`.
- **Agent chịu trách nhiệm:** `frontend-expert`
- **Gợi ý sửa:** thay 9 chỗ `#fff` bằng `var(--card)`; với 9 màu mới, đề
  xuất bổ sung token (`--surface-alt: #f8fafc`, `--line-strong: #cad4e1`,
  `--good-bg`/`--good-line`, `--bad-bg`/`--bad-line`) vào `styles.scss` rồi
  tham chiếu — **và báo cáo** danh sách token mới cho user như
  `ui-conventions.md:50-52` yêu cầu, không tự thêm im lặng.

### FE-3 — `.claude/docs/ui-conventions.md` § Testing
- **Mức: MISSING**
- **Bằng chứng:** toàn `src/FE/src` chỉ có **1** file spec — `app/app.spec.ts`
  (nguyên bản scaffold "should create the app"). Không test nào cho vùng
  quy ước gọi là rủi ro cao nhất: 5 mapper trong
  `modules/danh-muc-dti/services/danh-muc-dti.service.ts:30-87`, các mapper
  trong `modules/dashboard/services/dashboard.service.ts`,
  `core/services/api-response.service.ts`,
  `core/interceptors/http-error.interceptor.ts` (nơi FE-1 đang sai —
  1 test mapper 3 dòng đã bắt được).
- **Agent chịu trách nhiệm:** `frontend-expert`
- **Gợi ý sửa:** thêm `danh-muc-dti.service.spec.ts` (dùng
  `provideHttpClientTesting`, khẳng định `mapRowDto` giữ đủ 18 field và
  service unwrap đúng envelope) + `http-error.interceptor.spec.ts` cho
  FE-1. Không cần coverage cao — đúng 2 file này là đủ theo
  `ui-conventions.md:63-67`.

### FE-4 — `.claude/docs/architecture.md` § Bảng trách nhiệm (dumb component cấm "biết state global")
- **Mức: PARTIAL** (mức độ thấp — nhiều khả năng là quy ước thiếu ngoại lệ
  hơn là code sai)
- **Bằng chứng:** `shared/components/sidebar/sidebar.ts:22` và
  `shared/components/topbar/topbar.ts:18` inject `SidebarStateService`;
  `shared/components/toast/toast.ts:17` và
  `modules/dashboard/components/report-dialog/report-dialog.ts:23` inject
  `NotificationService` — cả 4 đều nằm trong `components/` (vùng "dumb"),
  trong khi bảng ở `architecture.md:30-36` ghi dumb component **cấm** "biết
  HTTP / state global".
- **Đánh giá công bằng:** đây là component **app-shell/hạ tầng UI**
  (sidebar, topbar, toast) — inject state layout/toast là hợp lý và phổ
  biến; quy ước chỉ đang thiếu 1 dòng ngoại lệ. Không có component nào
  inject **data service** (grep `HttpClient` chỉ ra `app.config.ts` + 2
  service của feature ✅) — tinh thần cốt lõi của quy tắc **không** bị vi
  phạm.
- **Agent chịu trách nhiệm:** `frontend-expert`
- **Gợi ý sửa:** chọn 1 — hoặc bổ sung vào `architecture.md` bảng trách
  nhiệm 1 dòng ngoại lệ tường minh cho "app-shell component (sidebar/
  topbar/toast) được inject service hạ tầng UI singleton, vẫn cấm data
  service"; hoặc chuyển 3 component shell ra khỏi vùng `components/`. Ưu
  tiên phương án đầu (rẻ, phản ánh đúng thực tế).

---

## PASS (tóm tắt)

**BE**
- `be/01` #1 — `BaseEntity` (`Id`/`CreatedAt`/`UpdatedAt`/`IsDeleted`) +
  soft-delete bằng **global query filter** đủ 4 entity nghiệp vụ, kèm unique
  index có filter `IsDeleted = false`; các chỗ `IgnoreQueryFilters()` đều
  cố ý và có comment giải thích. (Riêng phần setter → BE-1.)
- `be/01` #5 (nửa exception middleware) — bắt toàn cục, log đầy đủ phía
  server, response chỉ có `ErrorCode` + `TraceId`, **không lộ stack trace**.
- `be/01` #6 — envelope nhất quán ở **100%** action controller, kể cả
  endpoint list (`ApiResponse<PagedResult<T>>`) — đúng quyết định có chủ
  đích của `api-controller.md:70-73`. (Trừ nhánh model-binding → BE-7.)
- `be/07` — `TraceId` có mặt trong mọi response (dùng
  `HttpContext.TraceIdentifier` nhất quán).
- `be/09` § raw SQL — chỉ 4 chỗ `migrationBuilder.Sql` trong migration, SQL
  hằng, không ghép chuỗi từ input người dùng; toàn bộ query nghiệp vụ đi
  qua LINQ/EF (tự parameterize).
- `be/03` — đúng chủ trương "chỉ Loại (C)": `CriteriaGroup` là danh mục DB
  thuần, **không** có bảng metadata mô tả cột, **không** có engine grid
  generic — khớp `be/03:111-117`. (Trừ ngưỡng badge → BE-8.)
- CORS đúng `api-controller.md:104-109`: `WithOrigins` từ cấu hình, không
  `AllowAnyOrigin()`.

**FE**
- Tầng `core/` / `shared/` / `modules/` đúng `architecture.md`; không có
  logic riêng của feature nằm nhầm trong `core/`.
- Lazy route đúng chuẩn: `app.routes.ts:5-12` dùng `loadChildren`, mỗi
  feature export `*_ROUTES` riêng.
- **Ranh giới DTO ↔ model app làm rất chuẩn** — `*.dto.ts` giữ nguyên casing
  server, `I*` model riêng, mapper nằm trong `services/`
  (`danh-muc-dti.service.ts:30-87`), giữ 2 type kể cả khi trùng hệt; grep
  xác nhận **không component nào import DTO**.
- `HttpClient` chỉ xuất hiện ở `app.config.ts` + 2 service của feature —
  không rò rỉ vào page/component.
- Base URL qua `environment.ts` / `environment.development.ts`, không
  hardcode trong service; không có secret trong file environment.
- Angular 20 convention sạch tuyệt đối: grep `*ngIf|*ngFor|*ngSwitch|@Input(|@Output(|NgModule`
  → **0 kết quả**; mọi `@for` đều có `track` (grep `@for` không kèm `track`
  → 0 kết quả).
- Chống god component: file lớn nhất 382 dòng
  (`danh-muc-dti.page.ts`) — trong soft cap ~300-400; không có bản `-v2` song
  song.
- Interceptor lỗi HTTP dùng chung tồn tại và được đăng ký
  (`app.config.ts:13`), service feature không tự lặp lại logic bắt lỗi.
- API Contract Card có thật và được duy trì: `doc/contracts/danh-muc-dti.md`,
  `doc/contracts/dashboard.md`, đều `Status: IMPLEMENTED`, có ghi cả phần
  "khác biệt so với DRAFT" — đây là điểm sáng về hợp đồng cross-boundary.

---

## Chưa tới ngưỡng — KHÔNG tính là finding (theo dõi tiếp)

| Quy tắc | Ngưỡng theo wiki | Trạng thái hiện tại |
| --- | --- | --- |
| `be/01` #8 Caching | "khi có query nặng lặp lại" | 62 chỉ tiêu, DB local — chưa có query nặng |
| `be/01` #11/#12 Grid engine / Widget engine | "≥5-10 màn CRUD" / "≥2-3 dashboard" | 2 màn hình; wiki `be/01:32` ghi rõ **cố tình không làm** |
| `be/01` #13/#14/#17/#18 Notification/File storage/Background job/i18n | nhiều kênh / production-ready / tác vụ nền / đa ngôn ngữ | chưa có nhu cầu nào |
| `be/01` #15 Import engine | "≥2 màn Import" | mới 1 màn (`ImportController` + `CsvImportService`) |
| `be/02` Identity đa-Process | "≥2 process" | 1 process (nhưng xem Q2) |
| `be/05` Integration event / Outbox | "≥2 module ghi chéo nhau" | 1 module |
| `be/07` Health check / metrics | "khi chuẩn bị lên production thật" | demo local (`Program.cs:39-45` migrate+seed lúc khởi động — bản thân nó cũng là dấu hiệu chưa production) |
| `be/09` Rate limiting | "production-ready tối thiểu" | chạy local; **sẽ thành blocker** ngay khi deploy ra môi trường dùng chung, vì `POST /api/import/csv` nhận file 20MB không auth |

---

## Ghi chú về chính wiki (không phải finding của agent nào)

`wiki-core/be/01-core-components.md:32` viết: "PlatformManager (bản demo hiện
tại) **đã có** #1, #2, #3, #5, #6, #9 (mức tối giản)". Đối chiếu code:

| Mục | Wiki nói | Thực tế |
| --- | --- | --- |
| #1 | đã có | ✅ đúng |
| #2 | đã có | ⚠️ chỉ có `DbContext`/`DbSet` của EF, không có repository/UoW riêng (BE-5) |
| #3 | đã có | ❌ **không có** factory method nào, setter public (BE-1) |
| #5 | đã có | ⚠️ chỉ có nửa middleware, **không có** `Result<T>` (BE-2) |
| #6 | đã có | ✅ đúng (trừ nhánh model-binding, BE-7) |
| #9 | đã có | ⚠️ chỉ log kỹ thuật, không có audit nghiệp vụ (BE-10) |

Đề nghị user cho phép cập nhật lại đúng 1 dòng này trong wiki sau khi Q1
được trả lời — reviewer **không tự sửa** vì đây là câu mô tả trạng thái do
user/`main` viết, và nội dung đúng của nó phụ thuộc quyết định ở Q1.

## Ghi chú resolve root

`{BE_ROOT}` được xác định qua `src/BE/PlatformManager.slnx` — định dạng
solution XML mới của .NET 9/10 — **không có file `*.sln`** nào trong repo.
Marker `*.sln` ghi trong `.claude/agents/core-reviewer.md` § STEP -1 (và
hướng dẫn scaffold ở `src/BE/CLAUDE.md:33-47`) vì vậy không khớp thực tế;
reviewer coi `.slnx` là tương đương về ý nghĩa và **tiếp tục review** thay
vì dừng, vì rõ ràng có code để review (1 csproj + 24 file `.cs`). Đề nghị
sửa marker thành `*.sln` hoặc `*.slnx`.

## Giới hạn phạm vi của lượt review này

1. **Phần FE không được đối chiếu với bộ quy tắc core dài hạn** —
   `wiki-core/fe/README.md` còn là stub (`fe/README.md:1, 28-35` liệt kê
   TODO). Reviewer dùng `src/FE/.claude/docs/*.md` làm chuẩn tạm đúng như
   stub chỉ định, **không tự bịa thêm quy tắc core FE** (state management,
   design-token layer, form engine, HTTP abstraction ở tầm dài hạn...).
   Vì vậy 4 finding FE **không** đại diện cho "mức độ tuân thủ core FE" —
   chỉ là tuân thủ quy ước thực thi hiện tại.
2. Không chạy `dotnet build`/`ng build` — đánh giá thuần tĩnh trên source.
   Các finding đều dựa trên đọc code, không dựa trên hành vi runtime quan
   sát được (trừ FE-1 và BE-7, suy ra trực tiếp từ so khớp field/contract).
3. Không xét `doc/spec/*`, `doc/ERD/*` như nguồn quy tắc core — chúng là
   spec nghiệp vụ, ngoài phạm vi "core".
