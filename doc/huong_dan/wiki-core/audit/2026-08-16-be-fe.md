# Core Compliance Report — 2026-08-16 — Phạm vi: BE + FE (audit sau đợt tái
# cấu trúc Core ↔ Module / Modular Monolith)

## Kết luận: PASS

Đây là audit thứ 4 trong ngày 2026-08-16 (sau `2026-08-16-be.md`,
`2026-08-16-fe.md`, `2026-08-16-fe-f3.md`), tập trung vào đợt tái cấu trúc lớn
vừa hoàn thành: BE tách 4 project → 8 project (`Core.{Domain,Application,
Infrastructure}` + `Modules.DtiWeekly.{Domain,Application,Infrastructure}` +
`Api` + `Tests/PlatformManager.ArchTests`), FE tách `platform/` (4 màn Core)
ra khỏi `modules/` (2 module nghiệp vụ) + thêm ESLint. Đối chiếu trực tiếp
`doc/kien-truc-core-module.md` (tài liệu quyết định CHỐT 2026-08-16) với code
thật — **không chỉ đọc code, đã build/test/lint thật** để xác nhận, không tin
báo cáo của `backend-expert`/`frontend-expert`.

**Kết quả chính**: ranh giới Core↔Module đúng 100% cả 2 phía, có bằng chứng
chạy được (không chỉ đọc tĩnh). 3/3 finding PARTIAL của audit BE trước
(`2026-08-16-be.md`) đã được sửa và verify lại tại chỗ. 1 finding PARTIAL của
audit FE F3 trước (`2026-08-16-fe-f3.md`, nullability `email`) cũng đã sửa.
Không phát sinh finding mới nào do đợt tái cấu trúc — cấu trúc mới **chặt hơn**
cấu trúc cũ (thêm 2 ArchTest thật + 1 ESLint gate thật), không có quy tắc nào
bị nới lỏng.

---

## Bằng chứng đã chạy thật (không chỉ đọc code)

### BE

- `dotnet build src/BE/PlatformManager.slnx` → **Build succeeded, 0
  Warning(s), 0 Error(s)** (8 project, đúng đồ thị phụ thuộc).
- `dotnet test src/BE/PlatformManager.slnx` → **Passed! Failed: 0, Passed:
  10, Skipped: 0, Total: 10** — gồm đúng 2 ArchTest mới theo
  `doc/kien-truc-core-module.md` §"2 ArchTest mới cần thêm"
  (`Core_MustNotReference_AnyModulesAssembly`,
  `Modules_MustNotReference_OtherModules`, trong
  `Tests/PlatformManager.ArchTests/CoreModuleBoundaryTests.cs`), cộng 4 test
  layer dependency (`LayerDependencyTests.cs`), 1 test encapsulation
  (`EntityEncapsulationTests.cs`), 3 test hành vi pipeline chạy MediatR thật
  (`PipelineBehaviorTests.cs`, không phải reflection thuần — dựng
  `ServiceCollection` + `IMediator` thật, gửi command giả lập qua 2 pipeline
  behavior để verify hành vi, không chỉ verify tồn tại).
- Đối chiếu **9 file `.csproj`** trực tiếp (không suy đoán từ tên project):
  đồ thị `ProjectReference` khớp CHÍNH XÁC bảng "Nguyên tắc phụ thuộc bắt
  buộc" trong `doc/kien-truc-core-module.md` — `Core.Domain` zero
  `ProjectReference`/`PackageReference`; `Core.Application` → `Core.Domain`;
  `Core.Infrastructure` → `Core.Application`+`Core.Domain`;
  `Modules.DtiWeekly.Domain` → `Core.Domain` (không gì khác);
  `Modules.DtiWeekly.Application` → `Core.Application`+`Core.Domain`+
  `Modules.DtiWeekly.Domain`; `Modules.DtiWeekly.Infrastructure` →
  `Core.Infrastructure`+`Modules.DtiWeekly.Application`+
  `Modules.DtiWeekly.Domain`; `Api` → cả 6 project (composition root duy
  nhất). Không có `ProjectReference` chéo nào từ `Core.*` sang `Modules.*`.
- `PlatformManagerDbContext.cs` (`Core.Infrastructure/Persistence/`) **không**
  hardcode assembly module nào — nhận
  `IEnumerable<EfConfigurationAssembly>` qua constructor injection, mỗi
  module (`Core.Infrastructure/DependencyInjection.cs:32`,
  `Modules.DtiWeekly.Infrastructure/DependencyInjection.cs:26`) tự
  `services.AddSingleton(new EfConfigurationAssembly(typeof(DependencyInjection).Assembly))`
  đăng ký assembly của chính mình; `OnModelCreating` lặp qua danh sách gọi
  `ApplyConfigurationsFromAssembly`. `PlatformManagerDbContextFactory.cs`
  (design-time `dotnet ef`) đặt đúng ở `Api` (nơi duy nhất được phép biết cả
  2 assembly), có comment giải thích rõ lý do không đặt ở
  `Core.Infrastructure`. Migration `20260816080535_InitialCreate.cs` xác
  nhận có đủ cả bảng Identity/`SysMenus`/`SysMenuRoles` (Core) lẫn
  `Criteria`/`CriteriaGroups`/`CriteriaAssessments`/`CriteriaEvidences`
  (Module) — chứng minh cơ chế multi-assembly `ApplyConfigurationsFromAssembly`
  hoạt động thật, không chỉ lý thuyết.
- `PeriodRangeCalculator.cs` đã chuyển đúng từ `Core.Application/Common/` (vị
  trí cũ, đã lộ vi phạm ranh giới theo chính `doc/kien-truc-core-module.md`
  §Vấn đề) sang `Modules.DtiWeekly.Application/Common/` — xác nhận bằng
  `find`.

### FE

- `npx ng build` → **xanh**, cảnh báo duy nhất là warning cũ đã biết
  (`sidebar.scss` vượt 897 bytes so ngưỡng 4kB, chưa chạm ngưỡng lỗi 8kB) —
  không phát sinh warning/error mới sau khi tách `platform/`.
- `npx ng lint` → **"All files pass linting."**
- `npx ng test --watch=false --browsers=ChromeHeadless` → **3/3 SUCCESS**
  (không giảm/tăng so với audit F3 trước — đúng dự kiến vì đợt này chỉ di
  chuyển thư mục, không thêm business logic).
- **Test G8 (`no-restricted-paths`) chạy thật, không chỉ đọc config** — dùng
  `eslint --stdin --stdin-filename` (không ghi file nào vào
  `{FE_ROOT}`, tuân thủ ranh giới "chỉ đọc") để giả lập 1 file trong
  `modules/dashboard/` import trực tiếp `modules/danh-muc-dti/models/...`:
  kết quả **lỗi thật** `Unexpected path "..." imported in restricted zone`
  đúng message tuỳ biến đã viết trong `eslint.config.js:19-23`. Test đối
  chứng: cùng file import từ `core/auth/auth.service.ts` → **không lỗi**
  (đúng thiết kế "vẫn cho phép import từ `core/`, `shared/`, `platform/`").
  Xác nhận gate **hoạt động thật**, không phải chỉ khai báo trong config mà
  chưa từng kích hoạt.
- Cấu trúc thư mục: `find src/app/modules -maxdepth 1` chỉ còn `dashboard/`,
  `danh-muc-dti/` (2 module nghiệp vụ); `find src/app/platform -maxdepth 1`
  có đủ `login/`, `doi-mat-khau/`, `quan-tri-nguoi-dung/`, `phan-quyen/` — 4
  màn Core đã chuyển hết, không còn màn nào lạc trong `modules/`.
- `app.routes.ts` — cả 4 `loadChildren` của màn Core đã trỏ đúng
  `./platform/...` (dòng 13, 17, 31, 35); route path (`/dashboard`,
  `/danh-muc/dti`, `/quan-tri/nguoi-dung`, `/quan-tri/phan-quyen`) khớp
  chính xác với `SysMenu` seed trong `CoreSeeder.cs:79-84` phía BE — xác nhận
  đợt di chuyển thư mục không làm vỡ hợp đồng route giữa BE (menu) và FE.
  Grep toàn `src/FE/src` không còn tham chiếu `modules/login`,
  `modules/doi-mat-khau`, `modules/quan-tri-nguoi-dung`, `modules/phan-quyen`
  nào (kể cả comment) — dọn sạch hoàn toàn, không còn 2 chỗ comment lỗi thời
  như dự kiến trong `doc/kien-truc-core-module.md`.

---

## Xác nhận sửa các finding PARTIAL của audit trước (đối chiếu code trực tiếp)

1. **BE Finding #2 (`2026-08-16-be.md`) — auth opt-in, không fail-closed** —
   ĐÃ SỬA ĐÚNG. `ApiControllerBase.cs:19` nay có `[Authorize]` ở mức class
   (comment giải thích rõ lý do, trích cả tên bug cũ `AuthController.Logout`
   làm ví dụ), mọi controller con kế thừa mặc định yêu cầu đăng nhập; chỉ
   `AuthController.Login` tường minh `[AllowAnonymous]`. `Logout`/`Me`/
   `ChangePassword` không cần khai lại, được bảo vệ tự động.
2. **BE Finding #3 — `Guid.NewGuid()` trực tiếp thay vì `EntityId.New()`** —
   ĐÃ SỬA ĐÚNG. Grep toàn `src/BE` (loại `obj`/`bin`) chỉ còn 1 chỗ
   `Guid.NewGuid()` thật (`UserLookupService.cs:38`, dùng làm chuỗi ngẫu
   nhiên cho mật khẩu tạm, không phải sinh Id — đúng ngoại lệ đã ghi nhận
   trước đó). Mọi nơi sinh Id entity (`UserAdminService.cs:50`,
   `UserLookupService.cs:30`, `CoreSeeder.cs:43,54`, cả 5 entity nghiệp vụ)
   đều dùng `EntityId.New()`.
3. **BE Finding #4 — `AuditInterceptor` không set `DateUpdate` lúc tạo mới**
   — ĐÃ SỬA ĐÚNG. `AuditInterceptor.cs:41-50`, nhánh `EntityState.Added` nay
   set cả 4 field (`DateCreate`/`UserCreate`/`DateUpdate`/`UserUpdate`), có
   comment trích thẳng `wiki-core/be/trien-khai/04-p3-platform-persistence.md
   §7.2`.
4. **FE Finding (`2026-08-16-fe-f3.md`) — nullability `email` không khớp DTO
   BE** — ĐÃ SỬA ĐÚNG. `core/auth/current-user.model.ts:7` và
   `platform/quan-tri-nguoi-dung/models/quan-tri-nguoi-dung.model.ts:8` nay
   khai `email: string | null`, khớp `string? Email` của BE
   (`CurrentUserInfo.cs:6`, `UserDto.cs:6`).

Tất cả finding PARTIAL trước đó (BE Finding #1 "thiếu test project" —
MISSING nặng nhất — đã giải quyết triệt để bằng việc thêm hẳn 1 test project
10 test chạy thật; 4 finding PARTIAL/MISSING khác của cả 2 phía) nay đều ở
trạng thái đã sửa, verify lại bằng đọc code trực tiếp, không dựa vào lời báo
cáo của agent thực thi.

---

## Findings

Không có finding MISSING/PARTIAL nào phát sinh từ đợt tái cấu trúc này.

---

## Quan sát (không phải finding — chưa tới ngưỡng hoặc thuộc phạm vi khác)

- **Test pyramid BE mới chỉ có tầng ArchTest** (`be/04-testing-strategy.md`)
  — 10 test hiện tại đều là kiểm tra kiến trúc (layer/boundary/encapsulation)
  + 1 nhóm test hành vi pipeline (MediatR thật). Chưa có Unit test tầng
  handler (mock repository) lẫn Integration test tầng EF/Postgres
  (Testcontainers). Đúng theo `be/04-testing-strategy.md`, tầng ArchTest là
  tầng "nên có tối thiểu 2-3 test cốt lõi **từ ngày đầu**" — đã vượt (10
  test); 2 tầng còn lại là "nên có" khi logic nghiệp vụ đủ phức tạp để cần
  bảo vệ bằng test, chưa phải "bắt buộc ngày đầu" — không đánh finding, ghi
  chú theo dõi cho lần audit sau khi có thêm module nghiệp vụ #2 (lúc đó áp
  lực hồi quy sẽ cao hơn, nên cân nhắc thêm unit test cho handler).
- **`be/01-core-components.md` dòng 32 vẫn nhắc tên artifact cũ
  `ApiResponse<T>`/`ExceptionMiddleware`** — đã lỗi thời từ trước (được ghi
  nhận ở `2026-08-16-be.md`), vẫn chưa cập nhật ở đợt này (file có sửa đổi
  khác theo git status nhưng không đụng dòng này). Đây là quan sát về tài
  liệu, không phải finding về code — ngoài phạm vi core-reviewer chỉnh sửa
  wiki, nhưng nêu lại để không bị quên nếu ai đó chỉnh sửa file này sau.
- **Chưa có CI wiring** (`.github/workflows` không có job nào chạy
  `dotnet test`/`ng lint`/`ng build`) — 2 ArchTest mới và ESLint gate G8 hiện
  chỉ bảo vệ khi người chạy tay `dotnet test`/`ng lint`. Ở quy mô demo hiện
  tại (1 người, chưa có PR review flow) chưa tới ngưỡng bắt buộc CI — nhưng
  đây chính là lúc giá trị của 2 ArchTest/G8 sẽ giảm nếu không có ai chạy
  tay đều đặn. Ghi chú theo dõi, không phải finding.
- **`doc/contracts/danh-muc-dti.md`/`dashboard.md` DRAFT** — đã ghi nhận ở
  `2026-08-16-be.md`, ngoài phạm vi "core" (đây là DTO nghiệp vụ, không phải
  cơ chế core) nên không lặp lại review sâu ở đây.

---

## PASS (tóm tắt)

### BE — Core ↔ Module boundary (trọng tâm đợt audit này)
- 9 `.csproj` đúng 100% đồ thị phụ thuộc đã CHỐT trong
  `doc/kien-truc-core-module.md`; `dotnet build` 0 warning/0 error.
- 2 ArchTest mới (`Core_MustNotReference_AnyModulesAssembly`,
  `Modules_MustNotReference_OtherModules`) tồn tại thật và **pass thật** qua
  `dotnet test` (10/10), không chỉ đọc code tĩnh.
- `PlatformManagerDbContext` không hardcode assembly module nào — cơ chế
  `EfConfigurationAssembly` + DI collection hoạt động đúng, xác nhận qua
  migration sinh ra đủ bảng của cả Core lẫn Module.
- `PeriodRangeCalculator.cs` đã dọn khỏi `Core.Application/Common` (vi phạm
  đã ghi trong chính tài liệu quyết định) sang đúng
  `Modules.DtiWeekly.Application/Common`.
- `PlatformManagerDbContextFactory` (design-time) đặt đúng ở `Api`, không ở
  `Core.Infrastructure` — tôn trọng ranh giới ngay cả ở tooling phụ trợ.

### FE — Core ↔ Module boundary
- `modules/` chỉ còn 2 module nghiệp vụ (`dashboard`, `danh-muc-dti`); 4 màn
  Core đã chuyển đủ sang `platform/`, không còn sót.
- ESLint gate G8 (`no-restricted-paths`) cấu hình đúng VÀ **hoạt động thật**
  (verify bằng test trực tiếp qua stdin, không chỉ đọc `eslint.config.js`) —
  chặn đúng import chéo `modules/<A>` → `modules/<B>`, vẫn cho phép import từ
  `core/`/`shared/`/`platform/`.
- `app.routes.ts` cập nhật đủ 4 `loadChildren`, khớp route với `SysMenu` seed
  phía BE — không vỡ hợp đồng menu.
- `ng build`/`ng lint`/`ng test` đều xanh sau đợt tái cấu trúc, không phát
  sinh lỗi/warning mới.

### Các mục core khác (đối chiếu lại, không đổi so với audit trước)
- Toàn bộ PASS đã liệt kê trong `2026-08-16-be.md` (envelope `IApiResult<T>`/
  `ErrorDescriptor`, camelCase JSON, pipeline behavior thứ tự, `BaseEntity`/
  factory method/soft-delete, Auth Identity + cookie session, CORS,
  migration/seeder) và `2026-08-16-fe.md` + `2026-08-16-fe-f3.md` (envelope
  FE, zoneless, sidebar động, theme token, guard/route theo `[Authorize]`
  thật của BE, contract Users/Permissions/Auth) vẫn đúng nguyên trạng sau khi
  di chuyển thư mục/tách project — đã verify lại bằng build/test thật, không
  chỉ giả định "không đụng thì không đổi".
